apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  database.host: localhost
  database.port: "5432"
  log.level: info
  config.json: |
    {
      "debug": true,
      "verbose": false
    }
