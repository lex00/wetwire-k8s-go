apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-storage
  namespace: default
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
