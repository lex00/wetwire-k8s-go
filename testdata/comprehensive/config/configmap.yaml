apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  database_url: postgres://db:5432/myapp
  log_level: info
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
    database:
      maxConnections: 100
      idleConnections: 10
